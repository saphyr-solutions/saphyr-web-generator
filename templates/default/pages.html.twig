{% extends "_html.html.twig" %}

{% block bodyClasses %} {{ current_page.values.classes_css_body.value }}  p_{{ current_page.values.url.value and current_page.values.url.value != '/' ? current_page.values.url.value : 'home' }}{% endblock %}
{% block content %}

    {% include 'includes/nav.html.twig' %}

    <section id="main" class="flex-shrink-0">
        {% for section in current_page.sections %}
                {{ include('sections/' ~ section.values.columns.value ~ '.html.twig', {current_section: section}, with_context = true) }}
        {% endfor %}
    </section>

    {% include 'includes/footer.html.twig' %}
{% endblock %}