{% import "/macros/tool.html.twig" as macros %}

<div  {{ macros.getEditableBloc(current_bloc) }} class="card shadow-light-lg lift lift-lg h-100 {{ macros.getBlocClasses(current_bloc) }}" style="{{ macros.getStyles(current_bloc, "")|trim }}"  data-aos="fade-up" data-aos-delay="200">
    <!-- Image -->
    {% if current_bloc.values.image.value %}
        {{ macros.img(current_bloc, "card-img-top") }}

        <!-- Shape -->
        <div class="position-relative">
            <div class="shape shape-bottom shape-fluid-x text-white">
                <svg viewBox="0 0 2880 48" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M0 48h2880V0h-720C1442.5 52 720 0 720 0H0v48z" fill="currentColor"></path></svg>
            </div>
        </div>
    {% endif %}

    <!-- Body -->
    <div class="card-body">
        {% if current_bloc.values.label_1_label.value %}
            <div class="position-relative text-end mt-n8 me-n4 mb-3">
                {% if current_bloc.values.label_1_label.value %}
                    {{ macros.label(current_bloc, 1) }}
                {% endif %}
                {% if current_bloc.values.label_2_label.value %}
                    {{ macros.label(current_bloc, 2) }}
                {% endif %}
                {% if current_bloc.values.label_3_label.value %}
                    {{ macros.label(current_bloc, 3) }}
                {% endif %}
            </div>
        {% endif %}

        {% if current_bloc.values.icon_icon.value or current_bloc.values.icon_logo.value %}
            <div class="mb-5">{{ macros.icon(current_bloc) }}</div>
        {% endif %}

        {% if current_bloc.values.title.value %}
            {{ macros.header(current_bloc,2) }}
        {% endif %}

        {% if current_bloc.values.text.value %}
{#            <p style="{{ macros.getStyles(current_bloc, "text_")|trim }}" class="mt-5">{{ current_bloc.values.text.value|snl2br|raw }}</p>#}
        {% if isHTML(current_bloc.values.text.value) %}

            {{ current_bloc.values.text.value|raw }}

        {% else %}
            <p style="{{ macros.getStyles(current_bloc, "text_")|trim }}" class="mt-5">{{ current_bloc.values.text.value|snl2br|raw }}</p>
            {% endif %}
        {% endif %}

        {% if current_bloc.values.files %}
            {{ macros.files(current_bloc) }}
        {% endif %}

        {% if current_bloc.values.author.value %}
            <p class="mt-2 pt-2 border-top text-muted text-right fst-italic">{{ current_bloc.values.author.value|snl2br|raw }}</p>
        {% endif %}

        {% if current_bloc.values.button_text.value %}
            <div class="mt-5">
                {{ macros.button(current_bloc) }}
            </div>
        {% endif %}
    </div>
</div>