<footer class="py-8 py-md-11 bg-gray-200">
    <div class="container">
        <div class="row">
            <div class="col-12 col-md-4 col-lg-3">
                <!-- Brand -->
                {% if web.logo %}
                    <img src="{{ asset(web.logo) }}" alt="{{ web.title }}" class="footer-brand img-fluid mb-2">
                {% endif %}

                <!-- Social -->
                <ul class="list-unstyled list-inline list-social mb-6 mb-md-0">
                    {% if web.twitter %}
                        <li class="list-inline-item list-social-item me-3">
                            <a href="{{ web.twitter }}" class="text-decoration-none">
                                <img src="{{ asset("assets/dist/img/icons/social/twitter.svg") }}" class="list-social-icon">
                            </a>
                        </li>
                    {% endif %}
                    {% if web.facebook %}
                        <li class="list-inline-item list-social-item me-3">
                            <a href="{{ web.facebook }}" class="text-decoration-none">
                                <img src="{{ asset("assets/dist/img/icons/social/facebook.svg") }}" class="list-social-icon">
                            </a>
                        </li>
                    {% endif %}
                    {% if web.instagram %}
                        <li class="list-inline-item list-social-item me-3">
                            <a href="{{ web.instagram }}" class="text-decoration-none">
                                <img src="{{ asset("assets/dist/img/icons/social/instagram.svg") }}" class="list-social-icon">
                            </a>
                        </li>
                    {% endif %}
                    {% if web.linkedin %}
                        <li class="list-inline-item list-social-item me-3">
                            <a href="{{ web.linkedin }}" class="text-decoration-none">
                                <img src="{{ asset("assets/dist/img/icons/social/linkedin.svg") }}" class="list-social-icon">
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>

            <div class="col">
                <!-- List -->
                <ul class="list-unstyled text-muted mb-6 mb-md-8 mb-lg-0">
                    {% for page in menu|slice(0,5) %}
                        <li>
                            <a href="/{{ page.url }}" class="text-reset">{{ page.menu }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div> <!-- / .row -->

        <div class="row mt-5 pt-5 border-top border-gray-300 text-gray-500">
            <div class="col">
                {% if web.copyright %}
                    {{ web.copyright|raw }}
                {% else %}
                    &copy; Copyright <strong><span>{{ web.title }}</span></strong>
                {% endif %}
            </div>
            <div class="col text-end">
                by <a href="https://www.saphyr-solutions.com" target="_blank">Saphyr Solutions</a>
            </div>
        </div>

    </div> <!-- / .container -->
</footer>